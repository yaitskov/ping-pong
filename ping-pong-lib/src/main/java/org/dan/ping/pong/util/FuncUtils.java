package org.dan.ping.pong.util;

import java.util.function.BiFunction;

public class FuncUtils {
    public static final BiFunction<Integer, Integer, Integer> SUM_INT
            = (a, b) -> a + b;
}
