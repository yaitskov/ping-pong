'use strict';

angular.
    module('auth').
    component('authForm', {
        templateUrl: 'auth/auth.template.html',
        controller: ['$routeParams', 'auth', function ($routeParams, auth) {

        }]});
